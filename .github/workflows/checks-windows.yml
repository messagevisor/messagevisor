name: Checks (Windows)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  ci:
    name: Checks
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"

      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Lint project
        working-directory: projects/project-1
        run: npx messagevisor lint

      - name: Test project
        working-directory: projects/project-1
        run: npx messagevisor test --onlyFailures
