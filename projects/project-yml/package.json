{
  "name": "@messagevisor/project-yml",
  "private": true,
  "version": "0.12.0",
  "description": "Minimal YAML starter project for Messagevisor",
  "scripts": {
    "lint": "messagevisor lint",
    "build": "messagevisor build",
    "test": "messagevisor test"
  },
  "dependencies": {
    "@messagevisor/cli": "0.12.0",
    "@messagevisor/module-icu": "0.12.0"
  }
}
